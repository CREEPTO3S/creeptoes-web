import styled from 'styled-components';

const style = {
  Columns: styled.div`
    min-height: 100vh;
  `,
  Column: styled.div``,
  Form: styled.form`
    cursor: -webkit-grabbing;
    cursor: grabbing;
    
    label {
      cursor: -webkit-grabbing;
      cursor: grabbing;
    }
  `,
};

export default style;
