import styled from 'styled-components';

const style = {
  Field: styled.div``,
  Label: styled.label``,
  Control: styled.div``,
  Input: styled.input``,
  Icon: styled.span``,
};

export default style;
